{
  "name": "favorite-repo",
  "version": "1.0.0",
  "description": "Browse and save your favorite reposites here!",
  "main": "src/index.js",
  "scripts": {
    "pre:deploy": "pretty-quick --staged",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon src/index.js",
    "dev": "nodemon --legacy-watch controllers --legacy-watch models --inspect=0.0.0.0:3000 src/index.js",
    "deploy": "now --public && now alias nodejs-memory-game.now.sh"
  },
  "keywords": [],
  "author": "Igor Guastalla de Lima",
  "license": "MIT",
  "dependencies": {
    "apollo-server-cache-memcached": "^0.6.1",
    "apollo-server-express": "^2.8.1",
    "axios": "^0.19.0",
    "cors": "^2.8.5",
    "express-graphql": "^0.9.0",
    "graphql": "^14.4.2",
    "mongoose": "^5.6.8"
  },
  "devDependencies": {
    "express": "^4.17.1",
    "nodemon": "^1.19.1"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm run pre:deploy"
    }
  }
}
